#include <thread>
#include <mutex>
#include <shared_mutex>

using namespace std;

double probability(int curr, int best, double temperature) {
    return 0.0;
}


void update() {
    //WriteLock w_lock(myLock);
}

